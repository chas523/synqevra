{
  "name": "fpl_thingsboard",
  "version": "1.0.0",
  "private": true,
  "scripts":  {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev:front": "turbo run dev --filter=front",
    "dev:api": "turbo run dev --filter=api"
  },
  "license": "ISC",
  "packageManager": "pnpm@10.26.0",
  "devDependencies": {
    "turbo": "^2.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
