# Import those files to the ThingsBoard server using System Administrator account

## Resources > Resources Library > Add Resource as LWM2M model

## Devices > Add new device > Set `Name` and set profile to `LWM2M` > in Credentials create unique `Endpoint Client Name` > Add