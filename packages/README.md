This directory is for shared packages/libraries used across apps.

Examples of future packages:
- @fpl/types (shared TypeScript types)
- @fpl/api-client (generated or typed client for the backend)
- @fpl/config (shared environment / feature flags)

Currently empty — add packages here when cross-app sharing is needed.
All packages here are automatically included in the pnpm workspace.

